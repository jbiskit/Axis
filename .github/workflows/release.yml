name: Release Axis

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      version:
        description: Existing version to build without publishing
        required: true
        default: "0.1.3"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    name: Build Windows release
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.head_commit.message, 'release: ')
    runs-on: windows-latest
    env:
      CARGO_TERM_COLOR: always

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Resolve release version
        id: release
        shell: bash
        env:
          DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          if [[ "$GITHUB_EVENT_NAME" == "push" ]]; then
            subject="$(git log -1 --format=%s)"
            if [[ ! "$subject" =~ ^release:\ ([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
              echo "Release commits must use: release: X.Y.Z" >&2
              exit 1
            fi
            version="${BASH_REMATCH[1]}"
            publish=true
          else
            version="${DISPATCH_VERSION#v}"
            publish=false
          fi
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid release version: $version" >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "publish=$publish" >> "$GITHUB_OUTPUT"

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: crates/axis-tauri/ui/package-lock.json

      - name: Install UI dependencies
        working-directory: crates/axis-tauri/ui
        run: npm ci

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2.9.1
        with:
          workspaces: ". -> target"

      - name: Verify version consistency
        run: node scripts/verify-release-version.mjs "${{ steps.release.outputs.version }}"

      - name: Extract release notes
        run: node scripts/extract-release-notes.mjs "${{ steps.release.outputs.version }}" "${{ runner.temp }}/axis-release-notes.md"

      - name: Type-check UI
        working-directory: crates/axis-tauri/ui
        run: npx tsc --noEmit --pretty false

      - name: Build portable executable
        working-directory: crates/axis-tauri
        run: npx --yes @tauri-apps/cli@2.11.4 build --ci -- --locked

      - name: Test Rust workspace
        run: cargo test --workspace --release --locked

      - name: Generate checksum
        shell: bash
        run: |
          cd target/release
          sha256sum axis.exe > axis.exe.sha256

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v6
        with:
          name: axis-${{ steps.release.outputs.version }}-windows
          path: |
            target/release/axis.exe
            target/release/axis.exe.sha256
          if-no-files-found: error

      - name: Publish GitHub release
        if: steps.release.outputs.publish == 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.release.outputs.version }}
          name: Axis ${{ steps.release.outputs.version }}
          target_commitish: ${{ github.sha }}
          body_path: ${{ runner.temp }}/axis-release-notes.md
          fail_on_unmatched_files: true
          files: |
            target/release/axis.exe
            target/release/axis.exe.sha256
